<div class="container">
  <h1>Editing Post</h1>
  <%= form_for(@post, url: category_post_path(@category, @post), html: {method: "patch", multipart: true})  do |f| %>
  <%= render 'form', :f => f %>
  <% end %>

  <!-- <%= link_to 'Show', category_post_path(@post, @category) %> | -->
  <%= link_to 'Back', category_posts_path %>
</div>
