<div class="container">
  <h1>New Post</h1>
  <%= form_for(@post, :url => category_posts_path(Category.find(params[:category_id])), multipart: true) do |f| %>
  <%= render 'form', :f => f  %>
  <% end %>

  <%= link_to 'Back', category_posts_path %>
</div>
